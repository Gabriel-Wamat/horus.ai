# Horus.AI Docs

Fumadocs/Unmint documentation site for Horus.AI.

```bash
pnpm --filter @u-build/docs dev
pnpm --filter @u-build/docs build
```

Local docs URL:

```text
http://localhost:3002/docs
```
